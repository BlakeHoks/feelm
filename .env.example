# TMDB API key (v3 auth). Get one at https://www.themoviedb.org/settings/api
# Используется и в dev (Vite middleware), и в prod (Vercel Function).
# Важно: без префикса VITE_ — ключ не должен попадать в клиентский бандл.
TMDB_API_KEY=
